# Embedding Model (using Sentence Transformers - local, free)
# Model: all-MiniLM-L6-v2 (384 dimensions)
# No API keys required!

# Vector Store Settings
CHROMA_PERSIST_DIRECTORY=./chroma_db
COLLECTION_NAME=pdf_documents

# Chunking Settings
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
